services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-4173}:4173"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=4173
    restart: unless-stopped
